# Supply chain attack protection (axios incident 2026-03-30)
# postinstall scripts disabled by default to prevent malicious code execution
# Re-enable for specific trusted packages: npm install <pkg> --ignore-scripts=false
ignore-scripts=true
